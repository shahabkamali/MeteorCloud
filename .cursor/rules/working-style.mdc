---
description: How to change this repo — assumptions, scope, comments, tests
alwaysApply: true
---

# Working style

* State important assumptions.
* Modify only files relevant to the task.
* Keep code simple, human-readable, modular, and maintainable.
* Comment only non-obvious decisions, constraints, or behavior.
* Validate realistic inputs and edge cases without over-engineering.
* Run relevant tests, checks, and linters after making changes.
* Verify the requested behavior and avoid regressions.
* Deployment today is AWS-only: `installation.provider` is `Literal["aws"]` and apply/plan/destroy go through `AwsEc2Provider`. Do not treat Azure, GCP, Kubernetes, or on-prem as implemented.
* Multi-provider support is a future goal, not a current requirement. Keep using the existing AWS config contract (`aws:`, SSH, region, instance type, and related env secrets).
* When adding infra behavior, put AWS-specific code behind `InfrastructureProvider` / `AwsEc2Provider` (or a new adapter of that type). Do not scatter new provider APIs into app or Ansible as if other clouds already exist.
* Prefer containers and config/env for deploy-specific values. Avoid new hard-coded AWS account IDs, regions, or endpoints in application code.
* Never commit secrets. Use the target platform’s secret-management mechanism.
* Prefer portable, open standards unless an AWS-specific feature is explicitly required for the current path.

