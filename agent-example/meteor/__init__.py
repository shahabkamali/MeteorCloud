"""Meteor: the MeteorCloud device command-line agent.

The ``meteor`` command runs on managed Linux devices. It registers the device
with the control plane and (over time) will grow additional subcommands for
day-to-day device operations.
"""

from __future__ import annotations

__version__ = "0.1.0"
