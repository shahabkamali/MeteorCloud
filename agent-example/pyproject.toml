[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "edge-agent"
version = "0.1.0"
description = "Reference Linux agent for the Edge Platform fleet"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "paho-mqtt>=2.1.0",
]

[project.optional-dependencies]
dev = [
  "pytest>=8.3.0",
  "ruff>=0.9.0",
]

[project.scripts]
# `meteorcli` is the primary on-device command; `edge-agent` remains as an alias
# for the reference agent internals.
meteorcli = "meteorcli.cli:main"
edge-agent = "edge_agent.main:main"

[tool.hatch.build.targets.wheel]
packages = ["edge_agent", "meteorcli"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

[tool.ruff]
line-length = 120
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
